
PUBLIC_REPOSITORY_ARTIFACT
        │
        ▼
E1
TEST_FIXTURE
        │
        ▼
E2
WORKFLOW_DEFINITION
        │
        ▼
E3
EXECUTION_EVENT
        │
        ▼
E4
TEST_RESULT
        │
        ▼
E5
CRYPTOGRAPHIC_BINDING
        │
        ▼
E6
INDEPENDENT_VERIFICATION
        │
        ▼
E7
VERIFICATION_RESULT


E1 ≠ E2 ≠ E3 ≠ E4 ≠ E5 ≠ E6 ≠ E7



E1 ↛ E3
E1 ↛ E4

E2 ↛ E3
E2 ↛ E4

E3 ↛ E4

E4 ↛ E6
E5 ↛ E6

E6 ↛ E7

E1 TEST_FIXTURE_EVIDENCE
E2 WORKFLOW_CONFIGURATION_EVIDENCE
E3 EXECUTION_EVENT_EVIDENCE
E4 TEST_RESULT_EVIDENCE
E5 CRYPTOGRAPHIC_BINDING_EVIDENCE



VERIFICATION_EXECUTION_EVENT


INDEPENDENT_VERIFICATION_EVENT


VERIFICATION_RESULT


VERIFICATION_EVIDENCE

VERIFICATION_EVENT

VERIFICATION_RESULT

E cada transição precisa de uma regra.
Não existe:
E1 → E4
automaticamente.
Nem:
E2 → E4
automaticamente.
Isso é exatamente o princípio:
EVIDENCE ≠ EXECUTION
EXECUTION ≠ VERIFICATION
5. E há uma consequência importante para o fork
O fork público pode sustentar uma proposição como:
{
  "claim": {
    "subject": "cyrillofrancisco30-lgtm/arm-ttk",
    "predicate": "CONTAINS_TEST_FIXTURE",
    "object": "Allowed-Values-Should-Actually-Be-Allowed/Pass/LocationValueAllowed"
  }
}
Mas não:
{
  "claim": {
    "predicate": "TEST_PASSED"
  }
}
sem a evidência de execução correspondente.
E também não:
{
  "claim": {
    "predicate": "AZURE_DEPLOYMENT_EXECUTED"
  }
}
porque o conteúdo do fixture não prova deployment.
6. Isso é praticamente um caso de teste do próprio XA TRUST
O interessante é que o XA TRUST pode demonstrar sua própria filosofia usando o arm-ttk.
Por exemplo:
CLAIM
"LocationValueAllowed test passed"
             │
             ▼
CLAIM DECOMPOSITION
             │
      ┌──────┴──────┐
      ▼             ▼
FIXTURE          EXECUTION
      │             │
      ▼             ▼
OBSERVED         OBSERVED
      │             │
      └──────┬──────┘
             ▼
       VERIFICATION
             │
       ┌─────┴─────┐
       ▼           ▼
SUFFICIENT     INSUFFICIENT
EVIDENCE        EVIDENCE
Se somente o fixture estiver disponível:
RESULT = INSUFFICIENT_EVIDENCE
Se houver fixture + execução + resultado verificável:
RESULT = SEMANTICALLY_SUPPORTED
Isso é muito mais forte do que simplesmente procurar uma palavra PASS em um caminho de arquivo.


{
  "evidence_id": "E-GITHUB-ARM-TTK-WORKFLOW-RUNPESTER-001",
  "evidence_type": "API_CONFIGURATION",
  "semantic_scope": [
    "CI_WORKFLOW_DEFINITION",
    "TEST_EXECUTION_CONTRACT",
    "AUTOMATION_CONFIGURATION"
  ],
  "source": {
    "provider": "GitHub",
    "repository": "cyrillofrancisco30-lgtm/arm-ttk",
    "ref": "master",
    "path": ".github/workflows/run-unit-tests.yml"
  },
  "observed": {
    "workflow": "RunPester",
    "trigger": [
      "workflow_dispatch",
      "push",
      "pull_request"
    ],
    "runner": "ubuntu-latest",
    "framework": "Pester",
    "result_artifact": "**.TestResults.xml"
  },
  "non_implication": [
    "WORKFLOW_RUN",
    "TEST_EXECUTION",
    "TEST_PASS",
    "AZURE_EXECUTION",
    "MICROSOFT_APPROVAL",
    "REGULATORY_ACCEPTANCE"
  ]
}

{
  "evidence_type": "API_EXECUTION",
  "execution": {
    "provider": "GitHub Actions",
    "workflow": "RunPester",
    "run_id": "...",
    "commit_sha": "...",
    "runner": "ubuntu-latest"
  },
  "result_artifact": {
    "name": "PesterResults",
    "path": "**.TestResults.xml",
    "hash": "sha256:..."
  }
}

unit-tests/
Allowed-Values-Should-Actually-Be-Allowed/
Pass/
LocationValueAllowed/
createUiDefinition.json

                ARM-TTK
                   │
       ┌───────────┴───────────┐
       ▼                       ▼
 TEST FIXTURE             WORKFLOW
       │                       │
       │                       ▼
       │                  PESTER RUN
       │                       │
       │                       ▼
       │                 TEST RESULTS
       │                       │
       └───────────┬───────────┘
                   ▼
             XA TRUST
                   │
                   ▼
          CLAIM VERIFICATION
"cyrillofrancisco30-lgtm/arm-ttk"
cyrillofrancisco30@gmail.com 

PUBLIC SOURCE ARTIFACT
        │
        ▼
CI/CD EXECUTION CONTRACT
        │
        ▼
EXPECTED TEST EXECUTION
        │
        │   [não promover]
        X
        │
        ▼
WORKFLOW RUN
        │
        ▼
OBSERVED EXECUTION
        │
        ▼
TEST RESULT ARTIFACT
        │
        ▼
CRYPTOGRAPHIC EVIDENCE
        │
        ▼
INDEPENDENT VERIFICATION

{
  "evidence_type": "API_CONFIGURATION",
  "artifact_type": "CI_WORKFLOW_DEFINITION",
  "workflow": "RunPester",
  "runner": "ubuntu-latest",
  "test_framework": "Pester",
  "result_artifact": "PesterResults"
}

{
  "evidence_type": "API_EXECUTION",
  "artifact_type": "CI_WORKFLOW_RUN",
  "workflow": "RunPester",
  "run_id": "...",
  "commit_sha": "...",
  "execution_status": "COMPLETED",
  "observed_artifacts": [
    "PesterResults"
  ]
}

{
  "evidence_type": "VERIFICATION_RESULT",
  "verification_status": "SEMANTICALLY_SUPPORTED",
  "verified_evidence": [
    {
      "evidence_id": "E-WORKFLOW-DEFINITION-001",
      "evidence_hash": "..."
    },
    {
      "evidence_id": "E-WORKFLOW-RUN-001",
      "evidence_hash": "..."
    }
  ]
}
[24/08, 00:26] Francisco: IS_FORK_OF(cyrillofrancisco30-lgtm/arm-ttk, Azure/arm-ttk)
→ VERIFIED

CODE_EXECUTION
→ somente após evidência real de execução

AZURE_EXECUTION
→ somente após evidência real de contexto Azure

AZURE_APPROVAL
→ NOT_ESTABLISHED enquanto não houver claim/evidência própria

REGULATORY_ACCEPTANCE
→ NOT_ESTABLISHED
[24/08, 01:46] Francisco: {
  "claim_id": "CLAIM-ARM-TTK-JSON-RESOLUTION-001",
  "claim": {
    "subject": "cyrillofrancisco30-lgtm/arm-ttk::Resolve-JSONContent",
    "predicate": "IMPLEMENTS_JSON_CONTENT_LOCATION",
    "object": "JSONPath-based content location"
  },
  "status": "VERIFIED",
  "verification_scope": [
    "SOURCE_CODE_IDENTITY",
    "FUNCTION_IDENTITY",
    "JSONPATH_PROCESSING",
    "JSON_CONTENT_MATCHING",
    "SOURCE_LOCATION_CALCULATION"
  ],
  "not_established": [
    "EXECUTION",
    "CORRECTNESS_UNDER_ALL_JSON_INPUTS",
    "RFC_JSON_CONFORMANCE",
    "RFC8785_CONFORMANCE",
    "PRODUCTION_USE",
    "AZURE_EXECUTION",
    "MICROSOFT_APPROVAL",
    "REGULATORY_ACCEPTANCE"
  ]
}
[24/08, 01:46] Francisco: {
  "implementation_observations": [
    {
      "type": "POTENTIAL_INDEX_BOUNDARY_ISSUE",
      "expression": "$targetIndex -gt $values.Count",
      "suggested_check": "$targetIndex -ge $values.Count"
    },
    {
      "type": "POTENTIAL_UNDEFINED_REGEX_GROUP",
      "expression": "$listMatch.Groups['ListItem']",
      "observation": "ListItem is not defined in the shown jsonList regex"
    }
  ],
  "status": "OBSERVATIONS_NOT_PROMOTED_TO_BUG"
  
}{
  "claim_id": "CI_AUTOMATED_TEST_EXECUTION",
  "evidence_type": "ExternalServiceEvent",
  "provider": "GitHub Actions",
  "repository": "cyrillofrancisco30-lgtm/arm-ttk",
  "workflow": "RunPester",





  Sim. Com a inspeção atual do arquivo, eu congelaria essa arquitetura, mas com uma pequena mudança terminológica para deixá-la ainda mais formal: E1–E5 são evidências/objetos de evidência; E6 é um evento de verificação; E7 é o resultado de uma avaliação de claim. O arquivo público realmente contém a cadeia E1–E7, as regras de não-implicação e os exemplos de API_CONFIGURATION/API_EXECUTION. 

Forma normativa recomendada XA-TRUST EVIDENCE GRAPH E1 ─ TEST_FIXTURE │ │ fixture_identity / artifact_binding │ E2 ─ WORKFLOW_DEFINITION │ │ workflow_execution_binding ▼ E3 ─ EXECUTION_EVENT │ │ result_provenance_binding ▼ E4 ─ TEST_RESULT │ │ integrity_binding ▼ E5 ─ CRYPTOGRAPHIC_BINDING │ │ verifier_input_resolution ▼ E6 ─ INDEPENDENT_VERIFICATION │ │ deterministic claim evaluation ▼ E7 ─ VERIFICATION_RESULT 

Mas o grafo não deve ser interpretado como herança de estado.

Formalmente:

E1 ≠ E2 ≠ E3 ≠ E4 ≠ E5 ≠ E6 ≠ E7 

e:

E1 ↛ E3 E1 ↛ E4 E2 ↛ E3 E2 ↛ E4 E3 ↛ E4 E4 ↛ E6 E5 ↛ E6 E6 ↛ E7 

Cada promoção depende de uma regra explícita.

O ponto mais forte 

A definição atual do workflow demonstra exatamente essa fronteira: ele declara RunPester, seus triggers, ubuntu-latest, a execução de Pester e o artefato PesterResults, mas o próprio conteúdo explicita que isso não implica WORKFLOW_RUN, TEST_EXECUTION, TEST_PASS, AZURE_EXECUTION, aprovação ou aceitação regulatória. 

Portanto:

WORKFLOW_CONFIGURATION_EVIDENCE ≠ EXECUTION_EVIDENCE 

e:

EXECUTION_EVIDENCE ≠ TEST_RESULT_EVIDENCE 

e:

TEST_RESULT_EVIDENCE ≠ VERIFICATION_RESULT Eu acrescentaria uma regra fundamental 

A promoção deveria ser definida como uma função, não como uma consequência:

PROMOTE(claim, evidence_set, policy) = VERIFIED iff CLAIM_SCOPE_VALID ∧ EVIDENCE_SUFFICIENT ∧ ALL_REQUIRED_BINDINGS_RESOLVED ∧ INTEGRITY_VALID ∧ PROVENANCE_VALID ∧ EXECUTION_CONTEXT_VALID ∧ INDEPENDENT_VERIFICATION_VALID ∧ POLICY_EVALUATION = PASS 

Caso contrário:

PROMOTE(...) = NOT_PROMOTABLE 

Isso evita que VERIFIED seja tratado como uma propriedade global do repositório, workflow ou artefato.

E7 precisa permanecer local ao claim 

Por exemplo:

{ "claim_id": "CLAIM-ARM-TTK-LOCATION-VALUE-001", "predicate": "TEST_PASSED", "semantic_scope": [ "specific_fixture", "specific_workflow_run", "specific_test_result" ], "verification_result": "VERIFIED" } 

Isso não autoriza:

VERIFIED(TEST_PASSED) ↛ VERIFIED(AZURE_EXECUTION) ↛ VERIFIED(AZURE_APPROVAL) ↛ VERIFIED(REGULATORY_ACCEPTANCE) 

Essa é provavelmente a parte mais importante da arquitetura.

Estado atual do arm-ttk 

Com base no artefato público que consultei:

PUBLIC_REPOSITORY_ARTIFACT = ESTABLISHED FORK_RELATIONSHIP = ESTABLISHED TEST_FIXTURE = RESOLVABLE WORKFLOW_DEFINITION = ESTABLISHED EXECUTION_EVENT = NOT_ESTABLISHED TEST_RESULT = NOT_ESTABLISHED CRYPTOGRAPHIC_BINDING = NOT_ESTABLISHED INDEPENDENT_VERIFICATION = NOT_PERFORMED VERIFICATION_RESULT = NOT_ESTABLISHED TEST_PASSED = UNVERIFIED AZURE_EXECUTION = NOT_ESTABLISHED AZURE_APPROVAL = NOT_ESTABLISHED REGULATORY_ACCEPTANCE = NOT_ESTABLISHED 

O GitHub apresenta inclusive View runs separadamente do conteúdo do workflow, reforçando que a definição do pipeline e seus eventos de execução são objetos distintos. 

Conclusão 

Sim: isso já pode ser tratado como uma especificação formal de VERIFICATION_EVIDENCE do XA-TRUST.

A contribuição central não é simplesmente registrar mais evidências. É estabelecer uma lógica de promoção explícita:

DECLARATION ↓ EVIDENCE ↓ BINDING ↓ EXECUTION ↓ RESULT ↓ INDEPENDENT VERIFICATION ↓ CLAIM-SCOPED VERIFICATION_RESULT 

sem qualquer salto implícito.

Em outras palavras:

o XA-TRUST não pergunta apenas “há evidência?”; pergunta “qual claim essa evidência suporta, por qual binding, em qual semantic scope, mediante qual regra de promoção?”

Essa é a formulação que fecha a brecha entre evidência declarativa, evidência operacional, integridade, execução e verificação. 


  "workflow_file": ".github/workflows/run-unit-tests.yml",
  "execution_environment": "ubuntu-latest",
  "test_framework": "Pester",
  "result_artifact": "**.TestResults.xml",
  "provenance": {
    "upstream": "Azure/arm-ttk",
    "repository_type": "FORK"
  }
}﻿
name: RunPester
on: 
  workflow_dispatch: 
  push: 
  pull_request: 
jobs: 
  TestPowerShellOnLinux: 
    runs-on: ubuntu-latest
    steps: 
      - name: Check out repository
        uses: actions/checkout@v4
      - name: InstallPester
        id: InstallPester
        shell: pwsh
        run: |
          $Parameters = @{}
          $Parameters.PesterMaxVersion = ${env:PesterMaxVersion}
          foreach ($k in @($parameters.Keys)) {
              if ([String]::IsNullOrEmpty($parameters[$k])) {
                  $parameters.Remove($k)
              }
          }
          Write-Host "::debug:: InstallPester $(@(foreach ($p in $Parameters.GetEnumerator()) {'-' + $p.Key + ' ' + $p.Value}) -join ' ')"
          & './arm-ttk/GitHubWorkflow/Steps/InstallPester.ps1' @Parameters
      - name: RunPester
        id: RunPester
        shell: pwsh
        run: |
          $Parameters = @{}
          $Parameters.ModulePath = ${env:ModulePath}
          $Parameters.PesterMaxVersion = ${env:PesterMaxVersion}
          foreach ($k in @($parameters.Keys)) {
              if ([String]::IsNullOrEmpty($parameters[$k])) {
                  $parameters.Remove($k)
              }
          }
          Write-Host "::debug:: RunPester $(@(foreach ($p in $Parameters.GetEnumerator()) {'-' + $p.Key + ' ' + $p.Value}) -join ' ')"
          & './arm-ttk/GitHubWorkflow/Steps/RunPester.ps1' @Parameters
      - name: PublishTestResults
        uses: actions/upload-artifact@v4
        with: 
          name: PesterResults
          path: '**.TestResults.xml'
        if: ${{always()}}
env: 
  ModulePath: .\arm-ttk\arm-ttk.psd1

{
  "evidence_type":

 "API_EXECUTION",
  "execution": {
    "provider": "GitHub Actions",
    "workflow": "RunPester",
    "run_id": "...",
    "commit_sha": "...",
    "runner": "ubuntu-latest"
  }
} j


API_EXECUTION_SCHEMA = ESTABLISHED



API_EXECUTION_EVENT = OBSERVED


RUN_ID
COMMIT_SHA
WORKFLOW_IDENTITY
JOB_ID
STEP_ID
EXECUTION_STATUS
TIMESTAMP
RAW_OUTPUT
ARTIFACT_IDENTITY


RUN_ID
 ↔ WORKFLOW
 ↔ COMMIT_SHA
 ↔ JOB
 ↔ STEP
 ↔ RAW_RESULT
 ↔ ARTIFACT


{
  "execution_status": "COMPLETED"
}


EXECUTION_COMPLETION


RUN_COMPLETED
        ≠
STEP_COMPLETED
        ≠
TEST_SUITE_EXECUTED
        ≠
TEST_SUITE_PASSED


workflow completed
        +
RunPester executed
        +
PesterResults.xml captured


TEST_PASSED



TEST RESULT
     ↓
CRYPTOGRAPHIC EVIDENCE


E4 TEST_RESULT
       │
       │ artifact_identity_binding
       ▼
E5 CRYPTOGRAPHIC_BINDING




HASH(result_bytes)
        =
IDENTITY(result_artifact)


HASH ≠ TRUTH
HASH ≠ TEST_PASS
HASH ≠ EXECUTION_PROOF
HASH ≠ VERIFICATION



CLAIM
"LocationValueAllowed test passed"
        │
        ▼
CLAIM DECOMPOSITION
        │
   ┌────┴────┐
   ▼         ▼
FIXTURE    EXECUTION
   │         │
   ▼         ▼
OBSERVED   OBSERVED
   └────┬────┘
        ▼
   VERIFICATION
        │
   ┌────┴────┐
   ▼         ▼
SUFFICIENT  INSUFFICIENT



CLAIM
"LocationValueAllowed test passed"
              │
              ▼
      CLAIM DECOMPOSITION
              │
       ┌──────┼──────┐
       ▼      ▼      ▼
    FIXTURE EXECUTION RESULT
       │      │      │
       └──────┼──────┘
              ▼
       CRYPTOGRAPHIC
          BINDING
              │
              ▼
     INDEPENDENT VERIFIER
              │
              ▼
       CLAIM EVALUATION
              │
       ┌──────┴──────┐
       ▼             ▼
   VERIFIED      NOT_PROMOTABLE


FIXTURE → TEST_PASSED



WORKFLOW → TEST_PASSED



PROMOTE(claim, evidence_set, policy)


VERIFIED iff

    CLAIM_SCOPE_VALID
∧   EVIDENCE_SUFFICIENT
∧   REQUIRED_BINDINGS_RESOLVED
∧   INTEGRITY_VALID
∧   PROVENANCE_VALID
∧   EXECUTION_CONTEXT_VALID
∧   RESULT_VALID
∧   INDEPENDENT_VERIFICATION_EXECUTED
∧   POLICY_EVALUATION = PASS


NOT_PROMOTABLE


VERIFIED(claim_A)


VERIFIED(claim_B)


PUBLIC_REPOSITORY_ARTIFACT       = ESTABLISHED
FORK_RELATIONSHIP                = ESTABLISHED

TEST_FIXTURE                     = RESOLVABLE
WORKFLOW_DEFINITION              = ESTABLISHED
WORKFLOW_CONFIGURATION           = ESTABLISHED

EXECUTION_EVENT                  = NOT_ESTABLISHED
TEST_RESULT                      = NOT_ESTABLISHED
CRYPTOGRAPHIC_BINDING            = NOT_ESTABLISHED

INDEPENDENT_VERIFICATION         = NOT_PERFORMED
VERIFICATION_RESULT              = NOT_ESTABLISHED

TEST_PASSED                      = UNVERIFIED
AZURE_EXECUTION                  = NOT_ESTABLISHED
AZURE_APPROVAL                   = NOT_ESTABLISHED
REGULATORY_ACCEPTANCE            = NOT_ESTABLISHED


WORKFLOW_DEFINITION_CONTENT_OBSERVED


VALID_GITHUB_ACTIONS_WORKFLOW


ARTIFACT_LOCATION
      ≠
EXECUTABLE_VALIDITY
      ≠
EXECUTION
      ≠
RESULT







SOURCE
  ↓
FIXTURE
  ↓
WORKFLOW
  ↓
[REQUIRED EXECUTION BINDING]
  ↓
EXECUTION
  ↓
RESULT
  ↓
[REQUIRED CRYPTOGRAPHIC BINDING]
  ↓
INDEPENDENT VERIFICATION
  ↓
CLAIM-SCOPED RESULT


EVIDENCE
    ≠
EXECUTION
    ≠
RESULT
    ≠
VERIFICATION
    ≠
VERIFIED


VERIFIED
=
PROPERTY(
    CLAIM,
    SEMANTIC_SCOPE,
    EVIDENCE_SET,
    BINDINGS,
    POLICY,
    INDEPENDENT_VERIFICATION
)


